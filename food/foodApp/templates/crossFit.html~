<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Crossfit API Results Test Page</title>
  </head>
  <body>
    Testing results: </br>
    {% if d %}
      {% for key, value in d %}
        {{ key }} </br>
        {{ result }}
      {% endfor %}
    {% else %}
      Didn't recieve anything..
    {% endif %}
  </body>
</html>
